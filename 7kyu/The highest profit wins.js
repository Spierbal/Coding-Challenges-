const minMax = arr => [Math.max(...arr), Math.min(...arr)];

// Solution 1b:
/* function minMax(arr) {
  const max = Math.max(...arr);
  const min = Math.min(...arr);

  return [min, max];
} */

// console.log(minMax([1, 2, 3, 4, 5])); //[1, 5]],
// console.log(minMax([2334454, 5])); //[5, 2334454],
