const combineNames = (first, last) => `${first} ${last}`;

// Solution 2:
// const combineNames = (first, last) => first + " " + last;

//console.log(combineNames("James", "Stevens")); // "James Stevens"
